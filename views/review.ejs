<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Task <%= taskId %> Review</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      body {
        background-color: #0d1117;
        color: #e6edf3;
        font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI',
          sans-serif;
        padding: 1.5rem;
      }

      .card {
        background-color: #161b22;
        border: 1px solid #30363d;
        border-radius: 0.75rem;
        box-shadow: 0 10px 30px rgba(1, 4, 9, 0.4);
      }

      .diff-table {
        font-family: 'JetBrains Mono', ui-monospace, SFMono-Regular, 'SFMono-Regular',
          'Liberation Mono', Menlo, Monaco, Consolas, 'Courier New', monospace;
        font-size: 0.85rem;
      }

      .diff-line-addition {
        background: rgba(63, 185, 80, 0.18);
        color: #3fb950;
      }

      .diff-line-removal {
        background: rgba(248, 81, 73, 0.18);
        color: #f85149;
      }

      .diff-line-context {
        color: #8b949e;
      }

      .badge-violation {
        background-color: #f85149;
      }

      @media (max-width: 576px) {
        .card {
          padding: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <main class="container-fluid px-0">
      <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
        <div>
          <h1 class="h3 mb-1">Review – Task <span class="text-info"><%= taskId %></span></h1>
          <p class="mb-0 text-secondary">
            Latest diff validation snapshot with responsive layout for quick mobile audits.
          </p>
        </div>
        <span class="badge bg-success-subtle text-success-emphasis px-3 py-2">
          <strong>Mobile Optimized</strong>
        </span>
      </div>

      <% if (error) { %>
      <div class="alert alert-warning" role="alert">
        <%= error %>
      </div>
      <% } %>

      <% if (violations && violations.length > 0) { %>
      <div class="alert alert-danger" role="alert">
        <h2 class="h6 fw-semibold">Rule Violations</h2>
        <ul class="mb-0 ps-3">
          <% violations.forEach(function (violation) { %>
          <li><%= violation %></li>
          <% }) %>
        </ul>
      </div>
      <% } %>

      <div class="card p-3 p-sm-4">
        <div class="table-responsive rounded" style="max-height: 65vh;">
          <table class="table table-borderless table-hover align-middle diff-table mb-0">
            <thead>
              <tr class="text-uppercase text-secondary">
                <th scope="col" style="width: 3rem;">#</th>
                <th scope="col">Line</th>
              </tr>
            </thead>
            <tbody>
              <% if (diffLines.length === 0) { %>
              <tr>
                <td colspan="2" class="text-center text-secondary py-4">
                  No diff available yet.
                </td>
              </tr>
              <% } else { %>
              <% diffLines.forEach(function (line, index) { %>
              <% const rowClass =
              line.type === 'addition'
                ? 'diff-line-addition'
                : line.type === 'removal'
                ? 'diff-line-removal'
                : 'diff-line-context'; %>
              <tr class="<%= rowClass %>">
                <th scope="row" class="text-secondary fw-normal align-middle"><%= index + 1 %></th>
                <td class="text-break"><pre class="mb-0"><code><%= line.content %></code></pre></td>
              </tr>
              <% }) %>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </main>
  </body>
</html>
